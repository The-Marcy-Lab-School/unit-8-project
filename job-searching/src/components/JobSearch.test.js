import React from "react";
import { render, screen, fireEvent, getByText, waitForElement } from '@testing-library/react';
import App from "../App";

describe('render Job Component', () =>{
  test('render fake data from api', ()=>{
    
  })
});